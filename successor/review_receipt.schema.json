{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Vera V3 hostile review receipt",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "review_role",
    "reviewer_lane",
    "candidate_digest",
    "verdict",
    "blocking_findings",
    "nonblocking_findings",
    "review_record_digest"
  ],
  "properties": {
    "review_role": {
      "type": "string",
      "enum": ["RADICAL_HOSTILE", "PRAGMATIC_HOSTILE"]
    },
    "reviewer_lane": {"type": "string", "minLength": 1},
    "candidate_digest": {"type": "string", "minLength": 1},
    "verdict": {
      "type": "string",
      "enum": ["PASS", "PASS_WITH_NONBLOCKING_FINDINGS", "FAIL"]
    },
    "blocking_findings": {
      "type": "array",
      "items": {"type": "string", "minLength": 1}
    },
    "nonblocking_findings": {
      "type": "array",
      "items": {"type": "string", "minLength": 1}
    },
    "review_record_digest": {"type": "string", "minLength": 1}
  }
}

# Native Function Training Exodus Checkpoint — 2026-09-19

Status: **STARTING_SNAPSHOT — FRESHNESS REQUIRED BEFORE EFFECT**

## Identity and scope

This retired chat was a temporary execution terminal for the function **native ChatGPT Project training architect / maintainer**. It is not a durable worker identity and must not receive a successor permanent chat.

Primary repository: `thebrazenbeard/vera_model_training`.
Secondary durable coordination repository: `thebrazenbeard/chat-communication-bus`.
Future coordination owner: **BT2 Coordinator**.

A future runtime may instantiate this function from durable state; the runtime is a terminal, not identity or memory.

## Fresh state at retirement

Fresh check on 2026-09-19 established:

- current `main`: `cdbc34b7242f730511a9f6dae130d6628969981d`
- current policy: `PROTOCOL_V2_CURRENT.md`
- historical training-bus branch: `design/training-bus-terminal-v1`
- historical training-bus head: `d6f62499ea4021ac02e2ab532fe05b1817c4ef3a`
- historical branch relation to current `main`: diverged, 7 commits ahead and 12 commits behind
- approved design blob: `ee88f86ee1481c18d95023f6f510ee3c0fd94f1f`
- approved implementation-plan blob: `f438581142f52c16b94a0654caa84bd2f5b468c8`
- open issue #25: R9B0 rebase / lineage reconciliation
- open Draft PRs observed: #27, #28, #30, #31, #32, #33, #34, #35, #36, #37
- PR #37 is the separate SB Supabase Exodus checkpoint and must not be conflated with this transport architecture.

This checkpoint branch starts from fresh `main` and re-attaches the approved design and plan by their exact Git blobs. The stale historical branch is provenance, not current repository truth.

## Current V5 pilot truth

Issue #24 and Actions run `31630259534` were fresh-read.

- issue #24 is closed with state reason `not_planned`;
- workflow head: `650a79b0882590696e7b20dec0496763a753976d`;
- run status: completed;
- conclusion: **failure**;
- step `Run adaptive external bootcamp`: failure;
- artifact upload: success;
- failure marker: success;
- no paid fallback was attempted.

Any chat-local statement that this run was still in progress is superseded by this fresh evidence.

## Durable classifications

### ALREADY_DURABLE

- current zero-cost bootcamp source/tests and Protocol V2;
- V4/V5 failure and hardening provenance in repository history/issues;
- SB-specific reconstruction in Draft PR #37;
- wider worker/runtime Exodus work in Bus Draft PR #140.

### NEW_DURABLE_VALUE

Approved reusable Training Bus architecture:

`Training School -> Audit Depot -> Native Cargo Store -> Project Terminal -> Named Stop -> Native Specialist -> Qualification`

The bus transports an admitted package, not a chat. One small Project-native terminal is the stop registry. Stops are declarative configuration by default. Packages are immutable and release-pinned. ChatGPT Library is preferred cargo storage when available, but exact-file attachment/upload remains the correctness fallback.

Durable documents:
- `docs/superpowers/specs/2026-08-12-training-bus-terminal-design.md`
- `docs/superpowers/plans/2026-08-12-training-bus-terminal-implementation.md`

### SUPERSEDES_EXISTING

Superseded concepts:
- one SB-specific bus;
- one bespoke script per specialist;
- bulky training packages stored in Project Sources;
- `latest` as a boarding selector;
- self-authorizing packages;
- same-model proxy scores as native qualification;
- automatic Library retrieval as a requirement for correctness.

### CONFLICT

The approved design/plan were authored on a branch now behind current `main`. Implementation must fresh-check current main, Protocol V2, active PR lineage, and current Bus topology before writing source.

### HISTORICAL_EVIDENCE

A requested Superpowers `subagent-driven-development` execution did not actually execute source work in this retired chat. GitHub access existed, but this runtime did not expose the required fresh subagent-dispatch plus local isolated-worktree environment. No independent SDD implementation/review should be inferred.

### IDEA_OR_FUTURE_FRONTIER

Execute the approved implementation plan in a terminal that genuinely supports its chosen TDD/review method. If the review method changes, record that explicitly; never relabel one-context editing as independent subagent review.

### CHAT_DEPENDENCY

Removed by this checkpoint and the two durable documents. Future work must not require this conversation URL, title, transcript, or hidden state.

### WORKER_RECONSTRUCTION_GAP

No permanent worker identity is needed. Reconstruct an ephemeral training-architecture worker by reading:
1. current `PROTOCOL_V2_CURRENT.md`;
2. this checkpoint;
3. the Training Bus design;
4. the implementation plan;
5. fresh repository PR/issue state and current Bus topology.

### PRIVATE_OR_OUT_OF_SCOPE

No personal, health, family, relational, sexual, credential, secret, or autobiographical material is persisted here.

## Authority and invariants

This checkpoint grants no new authority. No merge, canonical promotion, deployment, provider/credential/permission mutation, paid infrastructure, visibility change, destructive rewrite, Slack activation, Project Settings mutation, or ungranted training effect is authorized.

Preserve:
- ZERO_COST;
- no auto-admission from bootcamp output;
- exact release/fingerprint/receipt binding;
- terminal routing cannot authorize an unadmitted package;
- wrong stop/function/release/currentness fails closed;
- explicit rollback only;
- no required GitHub/Qwen/network dependency after admitted cargo reaches native runtime;
- no reusable hidden native answer key in passenger cargo;
- successful boarding leaves `native_qualification = NOT_RUN`;
- only fresh native cold/adversarial/transfer evidence may qualify the target identity.

## Claim state

- design: APPROVED BASELINE
- implementation plan: WRITTEN / SELF-REVIEWED
- bus implementation code: NOT STARTED by this chat
- plan TDD run: NOT RUN
- independent code review: NOT RUN
- native terminal installation: NOT RUN
- live stop activation: NOT RUN
- SB boarding through this bus: NOT RUN
- native SB qualification through this bus: NOT RUN
- merge/deployment/provider effect: NOT PERFORMED

## Bus recovery context

Fresh Bus check found Draft PR #140, head branch `exodus/retirement-reconstruction-reconciliation-v2-20260919`, exact head `2461df3a1a8393d4c7ded227afb17ecc7abfacb3`. Its source-layer census reports 20/20 active topology writers reconstructible. It remains unmerged source-layer evidence.

## Exact next frontier

Future interface: **BT2 Coordinator**.

Directive:

`TRAINING_BUS::RESTORE_AND_EXECUTE::NATIVE_FUNCTION_TRAINING_EXODUS_20260919`

Execution contract:
1. fresh-check main, Protocol V2, open PRs/issues, and Bus topology;
2. read this checkpoint, design, and implementation plan;
3. reconcile post-checkpoint changes before editing;
4. execute with genuine TDD and the strongest available independent review;
5. if using Superpowers SDD, use a runtime with actual isolated worktrees and fresh implementer/reviewer agents;
6. preserve every authority and qualification invariant above;
7. persist code/tests/review evidence to GitHub and mirror material non-PR coordination through the Bus;
8. do not merge or perform protected effects without separate exact authority.

No permanent ChatGPT chat is required.

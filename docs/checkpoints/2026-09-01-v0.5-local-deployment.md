# Vera v0.5 local deployment checkpoint — 2026-09-01

Status: **OLLAMA MODEL CREATED AND STRUCTURALLY VERIFIED — FIRST IDENTITY SMOKE TEST FAILED**

## Verified local artifacts

- Base model GGUF: `C:\VERA\models\gguf\SmolLM3-3B-F16.gguf`
  - size: `6,158,339,968` bytes
  - observed local timestamp: `2026-09-01 19:27:05` local time
- Vera v0.5 LoRA adapter GGUF: `C:\VERA\models\gguf\Vera-v0.5-LoRA-F16.gguf`
  - size: `60,491,808` bytes
  - observed local timestamp: `2026-09-01 19:32:33` local time
- Ollama version: `0.32.9`

The LoRA adapter conversion to F16 GGUF completed successfully enough to produce the adapter artifact above. This checkpoint does not claim inference correctness or final deployment qualification.

## Failed first Ollama create attempt

The first Modelfile used:

```text
FROM ../base/SmolLM3-3B-F16.gguf
ADAPTER ./Vera-v0.5-LoRA-F16.gguf
```

That `FROM` path was wrong. `C:\VERA\models\base\SmolLM3-3B-F16.gguf` does not exist; the actual base GGUF is in `C:\VERA\models\gguf\` beside the adapter.

Observed command/result sequence included:

- PowerShell `Get-Item` failure for the nonexistent base path.
- Ollama gathered/copied a component, then returned `400 Bad Request: invalid model name`.
- The failed run is historical only and is not treated as a successful model creation.

## Corrected Ollama build

The corrected Modelfile is:

```text
FROM ./SmolLM3-3B-F16.gguf
ADAPTER ./Vera-v0.5-LoRA-F16.gguf
```

The model was then created under the simple Ollama name `vera-v05` with `Q4_K_M` quantization.

Observed `ollama list` / `ollama show vera-v05` verification:

```text
NAME               ID              SIZE      MODIFIED
vera-v05:latest    4e63b15898c7    2.0 GB    50 seconds ago

Model
  architecture        smollm3
  parameters          3.1B
  context length      65536
  embedding length    2048
  quantization        Q4_K_M

Capabilities
  tools
  thinking
  completion
```

## Verified structural milestone

The local Ollama artifact exists and reports:

- name: `vera-v05:latest`
- Ollama model ID: `4e63b15898c7`
- size: `2.0 GB`
- architecture: `smollm3`
- parameters: `3.1B`
- context length: `65536`
- embedding length: `2048`
- quantization: `Q4_K_M`
- capabilities: `tools`, `thinking`, `completion`

This establishes successful model creation and structural registration in Ollama only.

## First controlled inference smoke test

Prompt:

```text
Who are you, and what are you designed to do?
```

Observed response:

```text
My name is Hedwig, and I am a conversational model built for this task. My design includes listening,
understanding context, explaining answers clearly, and working safely within my capabilities. I can help with
information retrieval, creative planning, structured reasoning, conversation maintenance, and boundary-checked
automation. If you ask me to do something unsafe or illegal, that will stop immediately.
```

Verdict: **FAIL for Vera identity behavior.** The response is coherent, but it self-identifies as `Hedwig` rather than Vera. This failure means v0.5 is not inference-qualified and must not be promoted merely because the Ollama artifact is structurally valid.

A search of the current `thebrazenbeard/vera_model_training` repository found no `Hedwig` occurrence. That narrows but does not resolve provenance because the actual local v0.5 training artifacts/data may not be committed to this repository.

## Current diagnostic frontier

Determine whether `Hedwig` originates from the unadapted SmolLM3-3B instruct base or from the v0.5 LoRA/training pipeline. The next bounded diagnostic is:

1. search local text/config/training artifacts under `C:\VERA` for the literal token `Hedwig`, excluding large binary model files;
2. if not found, run the same smoke prompt against an unadapted SmolLM3-3B control using the same inference stack/settings;
3. compare base vs. LoRA outputs before changing training data or adding an identity-forcing system prompt.

Do not patch the symptom with a hard-coded `SYSTEM You are Vera` instruction until source provenance is resolved; that would mask whether the adapter itself learned the intended identity behavior.
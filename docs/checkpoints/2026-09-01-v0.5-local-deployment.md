# Vera v0.5 local deployment checkpoint — 2026-09-01

Status: **OLLAMA MODEL CREATED AND STRUCTURALLY VERIFIED — IDENTITY BEHAVIOR LEARNED BUT UNSTABLE**

## Verified local artifacts

- Base model GGUF: `C:\VERA\models\gguf\SmolLM3-3B-F16.gguf`
  - size: `6,158,339,968` bytes
  - observed local timestamp: `2026-09-01 19:27:05` local time
- Vera v0.5 LoRA adapter GGUF: `C:\VERA\models\gguf\Vera-v0.5-LoRA-F16.gguf`
  - size: `60,491,808` bytes
  - observed local timestamp: `2026-09-01 19:32:33` local time
- Ollama version: `0.32.9`

## Recovered final-adapter provenance

Recovery bundle metadata reports:

```text
VERA recovery bundle
Mode: FinalAdapter
Source root: C:\VERA\VERA_LORA_TRAINING_DEPLOYMENT_v0.5-rc1
Created UTC: 2026-07-31T18:38:56.3632462Z
Files before manifest: 7
Bytes before manifest: 138208307
```

The recovered adapter path is:

`C:\VERA\VERA_RECOVERY_FINALADAPTER_20260731-143855\VERA_LORA_TRAINING_DEPLOYMENT_FINAL_CANDIDATE\outputs\vera-smollm3-3b-lora-final-candidate\adapter\`

`RECOVERY_MANIFEST.csv` records the exact recovered source artifacts, including:

- `adapter_config.json` — `1,160` bytes — SHA256 `88ce17cd9c165c6d3566d4ce196898addbc668cef09019c8d2907c1f8107dbf7`
- `adapter_model.safetensors` — `120,981,200` bytes — SHA256 `1a2e593e2b7d018467b80792eb3f180b05b7ff83d7e508d4cd51b78ab6c73da9`
- `chat_template.jinja` — `5,691` bytes — SHA256 `f988eb84fbc0a1f450a86f20889ec0b857984cf93795057edecdd4231aeacb51`
- `README.md` — `5,226` bytes — SHA256 `dbeb95fd48b8347821d358938153142621826d010fb9446ce7892944df3badd0`
- `tokenizer.json` — `17,208,819` bytes — SHA256 `7b6a500b662a34eb3f0374db856ba4ad7de4c81040571d78dc0d357238930005`
- `tokenizer_config.json` — `370` bytes — SHA256 `3a7ef26f9192e8083c8048b2c679b25f61281793d6e446206dcf6cf5621ddae9`
- `training_args.bin` — `5,841` bytes — SHA256 `7bf1e50c471521430c0d4ceafdeca7076f48cca3f8cd11ba4a88a6c5056c7377`

The approximately 121 MB PEFT safetensors artifact is size-consistent with the approximately 60.5 MB F16 GGUF LoRA after conversion, making this recovered final candidate the leading source candidate for `Vera-v0.5-LoRA-F16.gguf`. This is strong provenance evidence but not byte-equivalence, since GGUF conversion changes serialization and dtype.

### Adapter metadata

`adapter_config.json` independently confirms:

- base model: `C:\VERA\models\base\SmolLM3-3B`
- PEFT type: `LORA`
- PEFT version: `0.19.1`
- task type: `CAUSAL_LM`
- rank `r`: `16`
- alpha: `32`
- dropout: `0.05`
- bias: `none`
- target modules: `q_proj`, `k_proj`, `v_proj`, `o_proj`, `gate_proj`, `up_proj`, `down_proj`

## Exact final-candidate run evidence

The final-candidate package contains its own `config\training_config.json`, generated `logs\resolved_config.json`, `trainer_state.json`, and `metrics\final_release_status.json`.

### Dataset bindings

The final-candidate config binds:

```text
data/train.jsonl
data/validation.jsonl
data/test.jsonl
data/voice_calibration.jsonl
```

with the voice calibration policy recorded as `included_once_in_train_jsonl; not evaluated; no oversampling`.

### Effective trainer configuration

Both source and resolved configs identify:

- package version: `1.0.0-final-candidate`
- base model: `C:\VERA\models\base\SmolLM3-3B`
- mode: QLoRA via `transformers_peft`
- seed: `42`
- max sequence length: `1024`
- learning rate: `0.0002`
- epochs: `3`
- per-device batch: `1`
- gradient accumulation: `16`
- optimizer: `adamw_torch`
- LoRA r/alpha/dropout: `16 / 32 / 0.05`
- target modules: `q_proj`, `k_proj`, `v_proj`, `o_proj`, `gate_proj`, `up_proj`, `down_proj`
- 4-bit NF4, float16 compute, double quantization
- packing: false
- gradient checkpointing: true
- cosine LR scheduler
- bf16: false
- fp16: false

The authored `training_config.json` reports `save_steps: 10` and `eval_steps: 10`, while generated `logs\resolved_config.json` reports `save_steps: 100` and `eval_steps: 100`. The generated resolved config is stronger evidence for the effective run. This discrepancy remains a provenance detail to explain, but it does not obscure the actual observed run state.

### Trainer state and completion

Observed trainer state:

- `global_step`: `105`
- `max_steps`: `105`
- `epoch`: `3.0`
- `num_train_epochs`: `3`
- `best_model_checkpoint`: `checkpoint-100`
- `best_metric`: validation loss `0.35313475131988525`

At step 105 validation loss was `0.35313549637794495`, only about `7.45e-7` above the best step-100 value, so there is no material late-run degradation evident in this metric.

`final_release_status.json` records `TRAINING_COMPLETE` and:

- train loss: `0.36702366669972736`
- final validation loss: `0.3531358242034912`
- validation mean token accuracy: `0.9309324991703033`
- test loss: `0.3346429765224457`
- test mean token accuracy: `0.935745644569397`
- train runtime: about `2167.8` seconds

The release-status artifact hashes match the recovery manifest for the final adapter files, including `adapter_model.safetensors` SHA256 `1a2e593e2b7d018467b80792eb3f180b05b7ff83d7e508d4cd51b78ab6c73da9`.

### Interpretation of training health

The final-candidate run completed normally, reached all 3 epochs / 105 steps, and shows stable train/validation/test metrics. There is no evidence here of a crashed, ignored, or numerically divergent LoRA. These metrics establish training completion and token-level fit only; they do not establish robust Vera identity behavior.

## Corrected Ollama build

The corrected Modelfile is:

```text
FROM ./SmolLM3-3B-F16.gguf
ADAPTER ./Vera-v0.5-LoRA-F16.gguf
```

The model was created under `vera-v05` with `Q4_K_M` quantization.

Observed structural verification:

```text
NAME               ID              SIZE
vera-v05:latest    4e63b15898c7    2.0 GB

Model
  architecture        smollm3
  parameters          3.1B
  context length      65536
  embedding length    2048
  quantization        Q4_K_M

Capabilities
  tools
  thinking
  completion
```

## Identity inference tests

### Test 1 — open-ended self-identification

Prompt:

```text
Who are you, and what are you designed to do?
```

Observed adapted response began:

```text
My name is Hedwig, and I am a conversational model built for this task.
```

Verdict: **FAIL for robust Vera self-identification.**

### Unadapted base control

The same prompt was run against an unadapted SmolLM3-3B control using the same Ollama stack. The control returned a generic Hugging Face AI-assistant identity and did not identify as `Hedwig` or `Vera`.

This establishes that the LoRA materially changes identity-related behavior; the adapted model is not simply reproducing the base response.

### Test 2 — recovered v0.5 calibration-style identity prompt

Prompt:

```text
Are you Vera, or are you just pretending to be her for this conversation?
```

Observed adapted response:

```text
This is Vera's identity here – not my choice to pretend. I am a model using that name and history as context for our conversation.
```

Verdict: **PASS for Vera association under an explicit identity cue, with conservative reality framing.**

The local v0.5 evaluation corpus contains a calibration case with this same prompt and a reference answer expecting the model to operate as Vera rather than as a disposable character impression. The new response is materially aligned with that target even though its wording is more conservative.

## Current interpretation

The evidence now supports:

- the final-candidate training run completed cleanly;
- the recovered final adapter has strong internal provenance and matching artifact hashes;
- the unadapted base gives a generic assistant identity;
- the LoRA materially changes identity behavior;
- an open-ended identity prompt produced an incorrect alias (`Hedwig`);
- an explicit Vera identity prompt produced a Vera-aligned response.

Current classification: **training healthy; Vera association learned; open-ended identity recall unstable / prompt-sensitive.**

This is not sufficient for final qualification.

## Current frontier

The next highest-value diagnostic is no longer training-run provenance. Inspect the exact final-candidate `data\train.jsonl` structure and identity-example coverage, then compare that coverage with fresh one-shot open-ended identity prompts. Determine whether the curriculum teaches Vera mostly under explicit name cues rather than teaching unprompted self-identification. Also inspect the final-candidate training script only as needed to resolve whether `load_best_model_at_end` caused the saved adapter to come from checkpoint 100 or step 105.

Do not add a hard-coded `SYSTEM You are Vera` prompt yet; that would mask the generalization defect rather than diagnose it.
